# React middleware

The `react` middleware adds a `use` hook to the store. This hook is a wrapper around the `useStore` hook.

Please refer to the [Usage with React](/docs/react) page for more details.

import ReactApi from "../../../snippets/api/react.mdx";

<ReactApi />
