# Sync middleware

The `sync` middleware synchronizes two stores together, keeping their state in sync automatically. This is useful for creating derived stores or connecting different parts of your application state.

Please refer to the [Derived stores](/docs/derived-stores) page for more details.

import SyncApi from "../../../snippets/api/sync.mdx";

<SyncApi />
